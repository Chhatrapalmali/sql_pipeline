name: deployment
on:   
   
   push:
   
jobs:
    build:
     name: Build Web    
  
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v3
       
       - name: Run a script
         run: docker run --rm -v "./:/flyway/sql" -v "./:/flyway/conf" dhoer/flyway:alpine migrate
         
       
